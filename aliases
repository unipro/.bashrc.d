# ~/.bashrc.d/aliases

# User specific aliases
alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'
alias diff='diff --strip-trailing-cr'
alias ec='emacsclient -c -a emacs'

# Local Variables:
# mode: shell-script
# End:
